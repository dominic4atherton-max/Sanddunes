[project]
name = "sanddunes"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "matplotlib",
  "pandas"
]

[project.scripts]
sanddunes = "sanddunes.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

